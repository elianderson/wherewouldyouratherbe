$ sudo apt-get install build-essential bison openssl libreadline6 \
libreadline5-dev curl git-core zlib1g zlib1g-dev libssl-dev \
libsqlite3-0 libsqlite3-dev sqlite3 libxml2-dev


$ cd
$ mkdir rvm
$ cd rvm
$ curl http://rvm.beginrescueend.com/releases/rvm-install-head > install
$ bash install


add this to your bash_profile [[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm"


$ which rvm

$ rvm install 1.9.2
$ rvm use 1.9.2
$ ruby -v
ruby 1.9.2p0 .......
$ rvm --default 1.9.2


$ gem install rails


http://rvm.beginrescueend.com/rvm/install/